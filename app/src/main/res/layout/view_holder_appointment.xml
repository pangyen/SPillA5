<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/standard_separator"
    android:layout_marginBottom="@dimen/standard_separator">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:src="@mipmap/medical" />

    <TextView
        android:id="@+id/tv_Atitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Appointment Title"
        android:ellipsize="end"
        style="@style/TextAppearance.AppCompat.Subhead"
        android:layout_alignTop="@id/iv_icon"
        android:layout_toEndOf="@id/iv_icon"
        android:layout_toRightOf="@id/iv_icon"
        android:paddingStart="@dimen/standard_separator"
        android:paddingEnd="@dimen/standard_separator"
        />

    <TextView
        android:id="@+id/tv_Adate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:text="Description"
        style="@style/TextAppearance.AppCompat.Caption"
        android:layout_toEndOf="@id/iv_icon"
        android:layout_toRightOf="@id/iv_icon"
        android:layout_below="@id/tv_Atitle"
        android:paddingStart="@dimen/standard_separator"
        android:paddingEnd="@dimen/standard_separator"
        />

    <TextView
        android:id="@+id/tv_Atime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:text="Time"
        style="@style/TextAppearance.AppCompat.Caption"
        android:layout_toEndOf="@id/iv_icon"
        android:layout_toRightOf="@id/iv_icon"
        android:layout_below="@id/tv_Adate"
        android:paddingStart="@dimen/standard_separator"
        android:paddingEnd="@dimen/standard_separator"
        />
    <TextView
        android:id="@+id/tv_Aplace"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:text="Place"
        style="@style/TextAppearance.AppCompat.Caption"
        android:layout_toEndOf="@id/iv_icon"
        android:layout_toRightOf="@id/iv_icon"
        android:layout_below="@id/tv_Atime"
        android:paddingStart="@dimen/standard_separator"
        android:paddingEnd="@dimen/standard_separator"
        />

</RelativeLayout>